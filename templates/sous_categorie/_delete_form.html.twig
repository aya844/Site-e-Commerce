<form onsubmit="return confirm('voulez vous vraiment supprimer cette sous catégorie')" method="post" action="{{ path('app_sous_categorie_delete', {'id': sous_categorie.id}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sous_categorie.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
