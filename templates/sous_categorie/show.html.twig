{% extends 'base.html.twig' %}

{% block title %}SousCategorie{% endblock %}

{% block body %}
    <div class="container">
        <br>
        <h1>Sous Catégorie</h1>
        <br>
        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sous_categorie.nom }}</td>
            </tr>
            </tbody>
        </table>

        <a class="btn btn-outline-primary" href="{{ path('app_sous_categorie_index') }}">Retour à la liste</a>

        <a class="btn btn-outline-primary" href="{{ path('app_sous_categorie_edit', {'id': sous_categorie.id}) }}">Modifier</a>

        {{ include('sous_categorie/_delete_form.html.twig') }}
    </div>


{% endblock %}
